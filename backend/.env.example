SECRET_KEY=django-insecure-insurex-bca-final-year-project-key-2026
DEBUG=True

# Database Configuration (MySQL)
# Set DB_ENGINE=mysql when connecting to MySQL server
DB_ENGINE=sqlite
DB_NAME=insurex_db
DB_USER=root
DB_PASSWORD=rootpassword
DB_HOST=localhost
DB_PORT=3306

# Allowed Hosts & CORS
ALLOWED_HOSTS=*
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
